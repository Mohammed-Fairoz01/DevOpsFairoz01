BEGIN
      DECLARE num;
      READ num;
      if num>=0;
      DISPLAY it's positive number;
      else if num < 0;
      DISPLAY it's Negative number;
      else 
      DISPLAY it's zer0;
      if num%2==0;
      DISPLAY it's even number;
      else
      DISPLAY it's odd number;
END