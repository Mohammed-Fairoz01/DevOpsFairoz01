find whether character is alphabet or digit or special character
BEGIN
      DECLARE a;
      READ a;
      if a==Character.isLetter
      DISPLAY it's alphabet ;
      else if a==Character.isDigit
      DISPLAY it's digit;
      else
      display it's special character;
END